import styles from "./Header.module.css"

const Header = () => {
  return (
    <header className={styles.header}>
      <h1 className="title-is-1">Ownit DEV</h1>
    </header>
  );
};

export default Header;
